<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.postgraduate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PresencesBsemesterProfessorActivity"></activity>
        <activity android:name=".PresencesAsemesterProfessorActivity" />
        <activity android:name=".PresencesBsemesterStudentActivity" />
        <activity android:name=".PresencesAsemesterStudentActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".CsemesterActivity" />
        <activity android:name=".BsemesterActivity" />
        <activity android:name=".AsemesterActivity" />
        <activity android:name=".SemesterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>